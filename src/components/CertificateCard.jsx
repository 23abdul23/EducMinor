const CertificateCard = ({ title = 'Certificate Title', description = 'Certificate description placeholder' }) => (
  <article className="section-placeholder">
    <h3>{title}</h3>
    <p>{description}</p>
  </article>
)

export default CertificateCard
