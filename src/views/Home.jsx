import CertificateCard from '../components/CertificateCard'
import Nav from '../components/Nav'

const Home = () => (
  <div>
    <Nav />
    <section className="section-placeholder">
      <h2>Dashboard Overview</h2>
      <p>Home view placeholder ready for integration.</p>
    </section>
    <CertificateCard />
  </div>
)

export default Home
